import 'package:flutter/material.dart';

class EcommerceThemeTokens {
  const EcommerceThemeTokens._();

  static const Color primary = Color(0xFFFF5722); // Vibrant High-Conversion Coral
  static const Color primaryLight = Color(0xFFFF8A65);
  static const Color accent = Color(0xFF2563EB); // Royal Blue for Secondary CTAs
  static const Color accentLight = Color(0xFF60A5FA);
  static const Color background = Color(0xFFF9FAFB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceElevated = Color(0xFFF3F4F6);
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color border = Color(0xFFE5E7EB);
  static const Color discountBadge = Color(0xFFDC2626);
  static const String fontFamily = 'Poppins';
}
