import 'package:flutter/material.dart';

class FoodDeliveryThemeTokens {
  const FoodDeliveryThemeTokens._();

  static const Color primary = Color(0xFFDC2626); // Appetite Warm Red
  static const Color primaryLight = Color(0xFFEF4444);
  static const Color accent = Color(0xFFF59E0B); // Golden Amber
  static const Color accentLight = Color(0xFFFBBF24);
  static const Color background = Color(0xFFFFFBEB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceElevated = Color(0xFFFEF3C7);
  static const Color textPrimary = Color(0xFF1C1917);
  static const Color textSecondary = Color(0xFF78716C);
  static const Color border = Color(0xFFFDE68A);
  static const String fontFamily = 'Outfit';
}
