import 'package:flutter/material.dart';

class HealthcareThemeTokens {
  const HealthcareThemeTokens._();

  static const Color primary = Color(0xFF0D9488); // Calming Medical Teal
  static const Color primaryLight = Color(0xFF14B8A6);
  static const Color accent = Color(0xFF3B82F6); // Soft Calming Blue
  static const Color accentLight = Color(0xFF93C5FD);
  static const Color background = Color(0xFFF0FDFA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceElevated = Color(0xFFCCFBF1);
  static const Color textPrimary = Color(0xFF134E4A);
  static const Color textSecondary = Color(0xFF5EEAD4);
  static const Color border = Color(0xFF99F6E4);
  static const String fontFamily = 'DM Sans';
}
